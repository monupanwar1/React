 1)props use for make the componets reuseblea let us suspose we have a componeta ansd we want 
 to use it many times then we use props  for this task we use props

 2) only same names use in props means the name we use in the passing the data in the in the componets must be same in the parameters of the componet;


 3)state only be change at the place only at the same place where it is creared;
 
